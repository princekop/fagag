# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =============================================================================
# This is a template for production environment variables
# Copy this file to .env.production and update with your production values
# NEVER commit .env.production to version control!
# =============================================================================

NODE_ENV="production"

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# IMPORTANT: Use PostgreSQL or MySQL in production, NOT SQLite
# PostgreSQL (Recommended):
DATABASE_URL="postgresql://username:password@host:5432/database?schema=public"

# MySQL Alternative:
# DATABASE_URL="mysql://username:password@host:3306/database"

# Connection pooling (recommended for production):
# DATABASE_URL="postgresql://username:password@host:5432/database?schema=public&connection_limit=10&pool_timeout=20"

# -----------------------------------------------------------------------------
# NextAuth Configuration
# -----------------------------------------------------------------------------
# CRITICAL: Must use HTTPS in production!
NEXTAUTH_URL="https://yourdomain.com"

# CRITICAL: Generate a secure 32+ character secret
# Command: openssl rand -base64 32
NEXTAUTH_SECRET=""

# -----------------------------------------------------------------------------
# Pterodactyl Panel Configuration
# -----------------------------------------------------------------------------
# Your production Pterodactyl panel URL (MUST be HTTPS)
PTERO_PANEL_URL="https://panel.yourdomain.com"

# Application API Key (Admin Panel -> Application API)
PTERO_API_KEY=""

# Client API Key (Account -> API Credentials)
PTERO_CLIENT_API_KEY=""

# Public panel URL for client-side links
NEXT_PUBLIC_PTERO_PANEL_URL="https://panel.yourdomain.com"

# -----------------------------------------------------------------------------
# Encryption Configuration
# -----------------------------------------------------------------------------
# CRITICAL: Exactly 32 characters for AES-256 encryption
# Command: openssl rand -base64 32 | cut -c1-32
PTERO_CRED_KEY=""

# -----------------------------------------------------------------------------
# Optional: Provisioning Settings
# -----------------------------------------------------------------------------
PTERO_EGG_ID=""
PTERO_DOCKER_IMAGE=""
PTERO_STARTUP=""
PTERO_LOCATION_IDS=""
PTERO_ENV_JSON='{}'

# -----------------------------------------------------------------------------
# Optional: Redis Configuration (Recommended for production)
# -----------------------------------------------------------------------------
# REDIS_URL="redis://localhost:6379"

# -----------------------------------------------------------------------------
# Optional: Monitoring & Analytics
# -----------------------------------------------------------------------------
# SENTRY_DSN=""
# SENTRY_ENVIRONMENT="production"
# NEXT_PUBLIC_ANALYTICS_ID=""

# -----------------------------------------------------------------------------
# Optional: Email Configuration (for notifications)
# -----------------------------------------------------------------------------
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER=""
# SMTP_PASSWORD=""
# SMTP_FROM="noreply@yourdomain.com"

# -----------------------------------------------------------------------------
# Optional: Rate Limiting
# -----------------------------------------------------------------------------
# RATE_LIMIT_MAX="100"
# RATE_LIMIT_WINDOW="900000"

# -----------------------------------------------------------------------------
# Optional: Feature Flags
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_ENABLE_ANALYTICS="true"
# NEXT_PUBLIC_ENABLE_MAINTENANCE_MODE="false"
