import { AuthPageSkeleton } from "@/components/auth-page-skeleton"

export default function Loading() {
  return <AuthPageSkeleton heading="Welcome back" subheading="Checking your credentials" ctaLabel="Preparing login" />
}
