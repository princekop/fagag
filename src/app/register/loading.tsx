import { AuthPageSkeleton } from "@/components/auth-page-skeleton"

export default function Loading() {
  return <AuthPageSkeleton heading="Create your account" subheading="Setting up your control panel" ctaLabel="Preparing signup" />
}
